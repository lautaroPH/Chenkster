const AskChenkster = () => {
  return (
    <div className="py-4 px-4 flex flex-col items-center justify-center bg-gradient-to-b from-[#7bb6f469] to-slate-100  mt-4 mb-10 rounded-lg">
      <p className="font-semibold tracking-wider text-center font-lato text-chenkster-gray">
        Looking for something more specific?
      </p>
      <button className="px-2 py-1 mt-4 tracking-wider text-white bg-gradient rounded-xl font-lato">
        Ask a chenkster
      </button>
    </div>
  );
};

export default AskChenkster;
